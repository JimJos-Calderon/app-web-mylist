/** Columnas de `lists` expuestas al cliente (sin `discord_webhook_url`). */
export const LIST_SELECT_PUBLIC =
  'id, name, description, owner_id, created_at, updated_at, is_private, invite_code, deleted_at, theme' as const
